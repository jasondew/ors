require "spec_helper"

describe ORS::Commands::Deploy do

  context "#run" do

    it "should call update, migrate, then restart" do
#      mock(self).update
#      mock(self).migrate
#      mock(self).restart

#      deploy
    end

  end

end
